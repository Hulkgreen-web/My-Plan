<Window x:Class="MyPlan.Accueil"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Accueil - My Budget Planner"
        Height="400" Width="600"
        Background="#f5f7fa"
        FontFamily="Segoe UI"
        FontSize="14">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Titre -->
        <TextBlock Text="Bienvenue dans My Budget Planner" 
                   FontSize="24" FontWeight="Bold" 
                   Foreground="#222"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,20"/>

        <!-- Contenu principal -->
        <StackPanel Grid.Row="1" VerticalAlignment="Top" HorizontalAlignment="Center" Width="400">
            <TextBlock Text="Gérez facilement vos revenus et dépenses." 
                       FontSize="16" Margin="0,0,0,15" TextAlignment="Center"/>

            <Button Content="Commencer à gérer mon budget" 
                    Width="250" Height="40" 
                    Background="#4CAF50" Foreground="White" 
                    FontWeight="SemiBold"
                    Cursor="Hand"
                    Click="StartBudget_Click"/>

            <Button Content="Quitter" 
                    Width="250" Height="40" Margin="0,15,0,0"
                    Background="#F44336" Foreground="White" 
                    FontWeight="SemiBold"
                    Cursor="Hand"
                    Click="Quit_Click"/>
        </StackPanel>
    </Grid>
</Window>
