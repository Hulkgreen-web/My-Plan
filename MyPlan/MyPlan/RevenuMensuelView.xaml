<Window x:Class="MyPlan.RevenuMensuelView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        Title="Dépenses Mensuelles" Height="550" Width="950"
        Loaded="Window_Loaded"
        Background="#FFF9F5" FontFamily="Georgia" FontSize="14"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <!-- Style pour les ComboBox -->
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#FFCC80"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <!-- Style pour le DataGrid -->
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#FFCC80"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#FFF5E5"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
        </Style>

        <!-- Style pour les en-têtes de colonnes du DataGrid -->
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#FFA726"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Titre -->
            <RowDefinition Height="Auto"/>
            <!-- Filtres -->
            <RowDefinition Height="*"/>
            <!-- Contenu -->
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- En-tête -->
        <Border Grid.Row="0" Grid.ColumnSpan="2" 
                Background="#FFA726" Padding="10" Margin="0,0,0,10">
            <TextBlock Text="Analyse des Revenus Mensuels" 
                       FontSize="20" 
                       FontWeight="Bold" 
                       Foreground="White"
                       HorizontalAlignment="Center"
                       FontFamily="Verdana"/>
        </Border>

        <!-- Filtres - Maintenant sur une ligne séparée -->
        <StackPanel Grid.Row="1" Grid.ColumnSpan="2" 
                   Orientation="Horizontal" 
                   HorizontalAlignment="Center" 
                   Margin="0,0,0,15">
            <TextBlock Text="Mois :" VerticalAlignment="Center" 
                      Foreground="#E65100" FontWeight="Bold"/>
            <ComboBox x:Name="MoisComboBox" Width="120" 
                      SelectionChanged="ComboBoxDate_SelectionChanged" 
                      Margin="5,0"/>

            <TextBlock Text="Année :" VerticalAlignment="Center" 
                      Foreground="#E65100" FontWeight="Bold" 
                      Margin="10,0,0,0"/>
            <ComboBox x:Name="AnneeComboBox" Width="120" 
                      SelectionChanged="ComboBoxDate_SelectionChanged" 
                      Margin="5,0"/>
        </StackPanel>

        <!-- DataGrid -->
        <Border Grid.Row="2" Grid.Column="0" Background="White" BorderBrush="#FFCC80" BorderThickness="1" Margin="0,0,10,0">
            <DataGrid x:Name="RevenusParCategorieDataGrid"
                      AutoGenerateColumns="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Catégorie" Binding="{Binding Nom}" Width="*"/>
                    <DataGridTextColumn Header="Montant estimé (€)" Binding="{Binding MontantEstime}" Width="*" ElementStyle="{StaticResource RightAlignedText}"/>
                    <DataGridTextColumn Header="Montant réel (€)" Binding="{Binding MontantReel}" Width="*" ElementStyle="{StaticResource RightAlignedText}"/>
                    <DataGridTextColumn Header="Écart (€)" Binding="{Binding Ecart}" Width="*" ElementStyle="{StaticResource RightAlignedText}"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Graphique -->
        <Border Grid.Row="2" Grid.Column="1" Background="White" BorderBrush="#FFCC80" BorderThickness="1" Padding="10">
            <lvc:PieChart x:Name="RevenusPieChart"
                          LegendLocation="Bottom" 
                          InnerRadius="20"
                          Foreground="#333">
                <lvc:PieChart.SeriesColors>
                    <lvc:ColorsCollection>
                        <Color>#FFA726</Color>
                        <Color>#FB8C00</Color>
                        <Color>#FFB74D</Color>
                        <Color>#FFCC80</Color>
                        <Color>#FFE0B2</Color>
                        <Color>#F57C00</Color>
                    </lvc:ColorsCollection>
                </lvc:PieChart.SeriesColors>
                <lvc:PieChart.ChartLegend>
                    <lvc:DefaultLegend Foreground="#333" FontFamily="Segoe UI"/>
                </lvc:PieChart.ChartLegend>
            </lvc:PieChart>
        </Border>
    </Grid>
</Window>