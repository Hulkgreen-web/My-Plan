<Window x:Class="MyPlan.MainWindow"
        Loaded="Window_Loaded"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyPlan"
        mc:Ignorable="d"
        Title="My Plan" Height="500" Width="800"
        Background="#f5f7fa" FontFamily="Segoe UI" FontSize="14">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Formulaire -->
        <StackPanel Grid.Row="0" Margin="0 0 0 10">

            <!-- Description -->
            <Grid Margin="0,0,0,8">
                <TextBox x:Name="DateBox" Padding="8" Background="White" BorderBrush="#ccc" BorderThickness="1"
             Foreground="#333" ToolTip="Saisissez la date (XX/XX/XXXX)."/>
                <TextBlock Text="Date" Margin="12,0,0,0" Foreground="#999" VerticalAlignment="Center" IsHitTestVisible="False"
               x:Name="PlaceholderDate"/>
            </Grid>
            
            <Grid Margin="0,0,0,8">
                <TextBox x:Name="DescriptionBox" Padding="8" Background="White" BorderBrush="#ccc" BorderThickness="1"
                         Foreground="#333" ToolTip="Saisissez une brève description de la transaction."/>
                <TextBlock Text="Description" Margin="12,0,0,0" Foreground="#999" VerticalAlignment="Center" IsHitTestVisible="False"
                           x:Name="PlaceholderDescription"/>
            </Grid>

            <!-- Montant -->
            <Grid Margin="0,0,0,8">
                <TextBox x:Name="MontantBox" Padding="8" Background="White" BorderBrush="#ccc" BorderThickness="1"
                         Foreground="#333" ToolTip="Saisissez un montant numérique. Utilisez une virgule si nécessaire."/>
                <TextBlock Text="Montant (€)" Margin="12,0,0,0" Foreground="#999" VerticalAlignment="Center" IsHitTestVisible="False"
                           x:Name="PlaceholderMontant"/>
            </Grid>

            <!-- Catégorie -->
            <Grid Margin="0,0,0,8">
                <ComboBox x:Name="CategorieComboBox" Width="200" 
                          DisplayMemberPath="Nom" SelectedValuePath="Id"
                          Background="White" BorderBrush="#ccc" BorderThickness="1"
                          ToolTip="Sélectionnez une catégorie pour classer cette transaction." />
                <Button Content="+" Width="30" Height="30" 
                        Margin="10,0,230,0"
                        Background="#FFC107" Foreground="White" 
                        FontWeight="Bold" FontSize="16" 
                        Cursor="Hand" Click="AjouterCategorie_Click" HorizontalAlignment="Right"
                        ToolTip="Cliquez pour ajouter une nouvelle catégorie."/>
            </Grid>

            <!-- Revenu -->
            <CheckBox x:Name="EstRevenuCheck" Content="Revenu" Margin="0,0,0,8"
                      Foreground="#444" FontWeight="SemiBold"
                      ToolTip="Cochez si c’est un revenu. Décochez pour une dépense."/>
        </StackPanel>

        <!-- Boutons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,0,15">
            <Button Content="Ajouter" Width="100" Margin="0,0,10,0" Padding="8"
                    Background="#4CAF50" Foreground="White" BorderThickness="0" 
                    Cursor="Hand" Click="Ajouter_Click"
                    ToolTip="Cliquez pour enregistrer la transaction."/>
            <Button Content="Supprimer" Width="100" Padding="8"
                    Background="#F44336" Foreground="White" BorderThickness="0" 
                    Cursor="Hand" Click="Supprimer_Click"
                    ToolTip="Supprime la transaction sélectionnée dans la liste."/>
            <Button Content="Menu principal" Width="180" Padding="8"
                    Background="#2196F3" Foreground="White" BorderThickness="0"
                    Cursor="Hand" Click="OpenMainMenu_Click" Margin="10,0,0,0"
                    ToolTip="Retourner à l’écran d’accueil."/>
        </StackPanel>

        <!-- Solde -->
        <TextBlock x:Name="SoldeText" Grid.Row="2"
                   FontWeight="Bold" FontSize="16"
                   HorizontalAlignment="Right" Foreground="#222"
                   Margin="0,0,0,10" ToolTip="Solde actuel (revenus - dépenses)"/>

        <!-- Liste des transactions -->
        <ListBox x:Name="ListeTransactions" Grid.Row="3" 
                 Background="White" BorderBrush="#ccc" BorderThickness="1"
                 Padding="5" ToolTip="Historique des transactions. Cliquez pour en sélectionner une."/>
    </Grid>
</Window>
