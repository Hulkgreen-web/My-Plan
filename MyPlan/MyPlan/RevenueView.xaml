<Window x:Class="MyPlan.RevenueView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        Title="Revenus" Height="700" Width="1000"
        Loaded="Window_Loaded"
        Background="#FFF9F5" FontFamily="Georgia" FontSize="14"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <!-- Styles réutilisés depuis les autres pages -->
        <Style TargetType="ComboBox" BasedOn="{StaticResource {x:Type ComboBox}}">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#FFCC80"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Height" Value="30"/>
        </Style>

        <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="8,4"/>
        </Style>

        <Style TargetType="DataGrid" BasedOn="{StaticResource {x:Type DataGrid}}">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#FFCC80"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="AlternatingRowBackground" Value="#FFF5E5"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>

        <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource {x:Type DataGridColumnHeader}}">
            <Setter Property="Background" Value="#FFA726"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,5"/>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Titre -->
            <RowDefinition Height="Auto"/>
            <!-- Filtres -->
            <RowDefinition Height="*"/>
            <!-- Contenu principal -->
            <RowDefinition Height="Auto"/>
            <!-- Bouton enregistrer -->
        </Grid.RowDefinitions>

        <!-- En-tête orange -->
        <Border Grid.Row="0" Grid.ColumnSpan="2" 
                Background="#FFA726" Padding="10" Margin="0,0,0,10">
            <TextBlock Text="Gestion des Revenus" 
                       FontSize="20" 
                       FontWeight="Bold" 
                       Foreground="White"
                       HorizontalAlignment="Center"
                       FontFamily="Verdana"/>
        </Border>

        <!-- Barre de filtre -->
        <DockPanel Grid.Row="1" LastChildFill="False" Margin="0,0,0,15">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                <TextBlock Text="Filtrer par catégorie :" 
                           VerticalAlignment="Center" 
                           Margin="0,0,10,0"
                           Foreground="#E65100" 
                           FontWeight="Bold"/>
                <ComboBox x:Name="CategorieFilterComboBox" Width="200"
                          DisplayMemberPath="Nom"
                          SelectedValuePath="Id"
                          SelectionChanged="CategorieFilterComboBox_SelectionChanged"/>
                <Button Content="Réinitialiser" 
                        Margin="10,0,0,0" 
                        Click="ResetFilter_Click"
                        Background="#FFB74D"/>
            </StackPanel>
            <Button Content="Accueil" DockPanel.Dock="Right"
                    Background="#FB8C00"
                    Click="RetourAccueil_Click"/>
        </DockPanel>

        <!-- Contenu principal : DataGrid + Graphique -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <!-- DataGrid -->
                <ColumnDefinition Width="2*"/>
                <!-- PieChart -->
            </Grid.ColumnDefinitions>

            <!-- DataGrid des revenus -->
            <Border Grid.Column="0" 
                    Background="White" 
                    BorderBrush="#FFCC80" 
                    BorderThickness="1" 
                    Margin="0,0,10,0">
                <DataGrid x:Name="ListeRevenus"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False" 
                          CanUserDeleteRows="False"
                          IsReadOnly="False"
                          RowEditEnding="ListeRevenus_RowEditEnding"
                          ItemsSource="{Binding RevenuList, RelativeSource={RelativeSource AncestorType=Window}}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Date" 
                                           Binding="{Binding Date, StringFormat=dd/MM/yyyy}" 
                                           IsReadOnly="True"/>
                        <DataGridTextColumn Header="Description" 
                                           Binding="{Binding Description}" />
                        <DataGridTemplateColumn Header="Catégorie">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding CategorieTransaction.Nom}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                            <DataGridTemplateColumn.CellEditingTemplate>
                                <DataTemplate>
                                    <ComboBox ItemsSource="{Binding DataContext.CategorieList, RelativeSource={RelativeSource AncestorType=Window}}"
                                              DisplayMemberPath="Nom"
                                              SelectedValuePath="Id"
                                              SelectedValue="{Binding CategorieTransaction.Id, Mode=TwoWay}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellEditingTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Montant estimé" 
                                           Binding="{Binding CategorieTransaction.montantEstime}" 
                                           IsReadOnly="True" />
                        <DataGridTextColumn Header="Montant (€)" 
                                           Binding="{Binding Montant}" />
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <!-- Graphique en camembert -->
            <Border Grid.Column="1" 
                    Background="White" 
                    BorderBrush="#FFCC80" 
                    BorderThickness="1" 
                    Padding="10">
                <lvc:PieChart x:Name="PieChartRevenus"
                              LegendLocation="Bottom"
                              InnerRadius="40"
                              Foreground="#333">
                    <lvc:PieChart.SeriesColors>
                        <lvc:ColorsCollection>
                            <Color>#FFA726</Color>
                            <Color>#FB8C00</Color>
                            <Color>#FFB74D</Color>
                            <Color>#FFCC80</Color>
                            <Color>#FFE0B2</Color>
                            <Color>#F57C00</Color>
                        </lvc:ColorsCollection>
                    </lvc:PieChart.SeriesColors>
                    <lvc:PieChart.ChartLegend>
                        <lvc:DefaultLegend Foreground="#333" FontFamily="Segoe UI"/>
                    </lvc:PieChart.ChartLegend>
                </lvc:PieChart>
            </Border>
        </Grid>

        <!-- Bouton Enregistrer -->
        <Button Grid.Row="3"
                Content="Enregistrer les modifications"
                Background="#FFB74D" 
                Click="Enregistrer_Click"
                HorizontalAlignment="Right"
                Margin="0,15,0,0"
                Width="220"
                Height="40"/>
    </Grid>
</Window>